import { LanguageStrategyService } from './language-strategy.service';

describe('LanguageStrategyService', () => {
  it('should create an instance', () => {
    expect(new LanguageStrategyService(null)).toBeTruthy();
  });
});
